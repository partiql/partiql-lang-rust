---
source: partiql/tests/pretty.rs
expression: doc
---
========================================================================================================================================================================================================
SELECT * FROM (g MATCH (x)-[e]->*(y) EXPORT ALL SINGLETONS )
========================================================================================================================================================================================================

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
SELECT * FROM (g MATCH (x) -[e]->* (y)
  EXPORT ALL SINGLETONS)

------------------------------------------------------------------------------------------------------------------------
SELECT * FROM (g MATCH (x) -[e]->* (y)
  EXPORT ALL SINGLETONS)

--------------------------------------------------------------------------------
SELECT * FROM (g MATCH (x) -[e]->* (y)
  EXPORT ALL SINGLETONS)

----------------------------------------
SELECT * FROM (g MATCH (x) -[e]->* (y)
  EXPORT ALL SINGLETONS)

------------------------------
SELECT *
FROM (g MATCH (x) -[e]->* (y)
  EXPORT ALL SINGLETONS)

--------------------
SELECT *
FROM (g MATCH (x) -[e]->* (y)
  EXPORT ALL SINGLETONS)

----------
SELECT *
FROM (g MATCH (x) -[e]->* (y)
  EXPORT ALL SINGLETONS)
